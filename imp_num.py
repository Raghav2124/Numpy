import numpy as np#alias method  to use a different name for numpy
arr=np.array([1,2,3,4,5])#creating an array using array() function also pass tuples 
print(arr)
print(type(arr))#show the type of an array 
print(np.__version__)#check the version of numpy we are using 
